const initialState ={
  username: '',
  profile_pic: '',
  landingAuth: false,
  newsAuth: false,
  eventsAuth: false,
  mediaAuth: false
};

//ACTION CONSTANTS


//ACTION BUILDERS


//REDUCER FUNCTION
export default function reducer(state = initialState, action) {
  switch(action.type) {

    default: return state;
  };
};