const initialState ={
  currUsername: '',
  currProfile_pic: '',
  currUser_id: 0,
  landingAuth: false,
  newsAuth: false,
  eventsAuth: false,
  mediaAuth: false
};

//ACTION CONSTANTS


//ACTION BUILDERS


//REDUCER FUNCTION
export default function reducer(state = initialState, action) {
  switch(action.type) {

    default: return state;
  };
};